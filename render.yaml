services:
  - type: web
    name: hopeful-ribbon-website
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn App.app:app
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: EMAIL_USER
        sync: false
      - key: EMAIL_PASSWORD
        sync: false
